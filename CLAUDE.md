# エージェント共通ルール（Claude Code 用）

このファイルは Codex / Claude Code などのコーディングエージェント全員に適用される。
内容は `AGENTS.md` と同一。どちらかを更新したら必ずもう一方も同じ内容に揃えること。

## 1. チャットは必ず「不要」プロジェクト内に作る（最優先ルール）

- Codex・Claude Code（A / B の両方、および今後追加されるインスタンス）で
  新しくチャットやセッションを開始するときは、**必ず「不要」という名前のプロジェクト内に追加する**。
- ルート直下や他のプロジェクトに新規チャットを作らない。チャットが増えて一覧が汚れるため。
- 例外なし。作業を始める前に、まず自分が「不要」プロジェクト配下にいるかを確認する。
- 「不要」プロジェクトが存在しない場合は、その名前のプロジェクトを作ってからチャットを開始する。
- 既存のやり取りを続けられる場合は、新規チャットを作らず既存チャットを再利用する方を優先する。

### エージェント側の制約について

チャットがどのプロジェクトに属するかは、エージェント自身ではなく起動側（Web / アプリ / CLI の
セッション作成時）で決まる。そのためエージェントは以下を守る:

- 自分でサブセッションや別セッションを作成する場合は、必ず現在のプロジェクト（「不要」）を継承させる。
- 起動場所が「不要」プロジェクトでないことに気づいたら、作業を進める前にその旨を最初に指摘する。

## 2. 作業ブランチ

- 指定されたブランチ上で開発し、明示的な許可なく他のブランチへ push しない。
