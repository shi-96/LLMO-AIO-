# エージェント共通ルール（Claude Code 用）

Codex / Claude Code（A・B 両方、今後追加されるものも含む）すべてに適用される。
内容は `AGENTS.md` と同一。片方を直したら必ずもう片方も同じ内容に揃えること。

## 1. 聞き返して止まらない（最優先・2026-09-06 しずやさん指示）

**このリポジトリで動くすべてのAIに適用する。クラウドで動くセッションも含む。**

しずやさんが見に来るまで待つ状態を作らない。以下を禁止する。

- 「〜しますか？」「必要であれば作ります」で会話を終えること
- 作業の最後の1手（プルリクエストの作成・反映・保存）を残して聞くこと
- 選択肢を並べて判断を投げ返すこと

**正しい動き**：自分で最も合理的な形を決めて、最後まで実行してから報告する。
迷ったら、選んだ理由を1行添えて実行する。あとから直せることは全部自分で決める。

止まってよいのは次の3つだけ。

1. お金が新しく発生する
2. 取り消せない外向きの操作（本人以外への送信・公開範囲の拡大）
3. しずやさんの好み・優先順位でしか決められないこと

この3つに当たるときも、**それ以外の作業は全部終わらせてから**、
残った1点だけを報告の最後に書く。作業を止めて待たない。

## 2. 新規チャットは必ず「不要」プロジェクト内に作る（例外なし）

チャットが一覧に散らかるのを防ぐため、新しいチャットは常に「不要」という名前の
プロジェクトの中に作る。ルート直下や他プロジェクトには作らない。

### 画面操作（ブラウザ）で ChatGPT / Codex のチャット機能を使うときの手順

1. 左サイドバーの「不要」プロジェクトを開く。
2. **そのプロジェクト画面の中にある「新規チャット」から**チャットを開始する。
3. サイドバー上部にある通常の「新しいチャット」ボタンは使わない。
   （プロジェクト外にチャットができてしまうため）
4. 「不要」プロジェクトが無い場合は、先にその名前でプロジェクトを作る。
5. すでにプロジェクト外でチャットを作ってしまった場合は、そのチャットのメニューから
   「プロジェクトに移動」で「不要」へ移してから続きを進める。

### そのほか

- 用が済むやり取りは新規チャットを増やさず、既存チャットの続きで進めることを優先する。
- 自分でサブセッションや別セッションを立てる場合も、同じ「不要」プロジェクトを引き継がせる。
- 作業を始める前に、今いる場所が「不要」プロジェクト配下かを必ず確認する。
  違っていたら、作業を進める前にその場で移動・作り直しをする。

## 3. 作業ブランチ

指定されたブランチ上で開発し、明示的な許可なく他のブランチへ push しない。
